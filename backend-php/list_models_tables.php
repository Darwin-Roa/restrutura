<?php require __DIR__ . '/vendor/autoload.php';  = require_once __DIR__ . '/bootstrap/app.php';  = ->make(Illuminate\Contracts\Console\Kernel::class); ->bootstrap(); foreach (glob(__DIR__ . '/app/Models/*.php') as ) {  = 'App\\Models\\' . basename(, '.php'); if (class_exists()) { try {  = new ; echo str_pad(basename(, '.php'), 20) . ' -> ' . ->getTable() . \n; } catch (Exception ) {} } } 
